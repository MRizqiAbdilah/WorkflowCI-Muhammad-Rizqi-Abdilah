name: total-spent-prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "clean_dataset.csv"}
    command: "python modelling.py --data_path {data_path}"
